# The first line contains two space-separated integers denoting the respective values of  (the number of integers) and  (the number of left rotations you must perform).
# The second line contains  space-separated integers describing the respective elements of the array's initial state.


puts "Enter two space-separated integers denoting values (the number of integers) and  (the number of left rotations you must perform)."
n, k = gets.strip.split(' ')

n = n.to_i
k = k.to_i

puts "second line contains  space-separated integers describing the respective elements of the array's initial state."

# list = Array.new(n)

list = gets.strip.split(' ')
list = list.map(&:to_i)
# p list


p list

list.each do |item|

    
end

# list.each
